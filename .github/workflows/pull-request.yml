# Pull Request Workflow
name: Pull Request Validation

# Define when the workflow should run
on:
  pull_request:
    branches:
      - master
    paths:
      - 'force-app/**'

# Job Definition
jobs:
  # Push the source to a scratch org and run tests
  scratch-org-test:
    runs-on: ubuntu-latest
    steps:
      # Checkout the code in the pull request
      - name: 'Checkout source code'
        uses: actions/checkout@v2

      # Install SFDX CLI
      - name: 'Install Salesforce CLI'
        run: |
          wget https://developer.salesforce.com/media/salesforce-cli/sfdx-linux-amd64.tar.xz
          mkdir sfdx-cli
          tar xJf sfdx-linux-amd64.tar.xz -C sfdx-cli --strip-components 1
          ./sfdx-cli/install

      # Install any DX plugins needed for the job
      - name: 'Install SFDX Plugins'
        run: |
          echo 'y' | sfdx plugins:install sfpowerkit
          echo 'y' | sfdx plugins:install @dxatscale/sfpowerscripts

      # Populate JWT Key file for dev hub
      - name: 'Populate keyFile.txt file with DEVHUB_JWT_KEY secret'
        shell: bash
        run: 'echo ${{ secrets.DEVHUB_JWT_KEY}} > ./keyFile.txt'

      # Authenticate dev hub
      - name: 'Authenticate Dev Hub'
        run: 'sfdx force:auth:jwt:grant -u ${{ secrets.DEVHUB_USERNAME }} -f ./keyFile.txt -i ${{ secrets.DEVHUB_CONSUMER_KEY }} -r ${{ secrets.DEVHUB_INSTANCE_URL }} -a devhub -d'

      # Create scratch org
      - name: 'Create scratch org'
        run: 'sfdx force:org:create -f config/project-scratch-def.json -a scratch-org -s -d 1'

      # Install any dependent packages if they exist in the sfdx-project.json
      - name: 'Install package dependencies'
        run: 'sfdx sfpowerkit:package:dependencies:install -u scratch-org -r -w 20'

      # Deploy source to scratch org
      - name: 'Push source to scratch org'
        run: 'sfdx force:source:push'

      # Run Apex tests in scratch org
      - name: 'Run Apex tests'
        run: 'sfdx force:apex:test:run -c -r junit -d ./tests/apex -w 20'
        env:
          SFDX_IMPROVED_CODE_COVERAGE: true

      # Upload code coverage and test run reports
      - name: 'Upload Test Results'
        uses: actions/upload-artifact@v2
        with:
          name: test-result
          path: tests/apex/test-result.xml

      - name: 'Upload Code Coverage Report'
        uses: actions/upload-artifact@v2
        with:
          name: codecoverage
          path: tests/apex/test-result-codecoverage.json

      # Validate Code Coverage
      - name: 'Validate Code Coverage in Scratch Org'
        run: 'sfdx sfpowerscripts:ValidateApexCoverage -u scratch-org -t 85'

      # Packaging Steps
      # 1. Create new beta version 
      #    (NOTE: We will skip coverage to save time since we validated it in the prior step.  
      #           We will add the coverage check to the version created in CI process job before we promote the package)
      # 2. Upload artifact for the install link that can be use to install the beta package
      - name: 'Create new package version'
        run: |
          sfdx sfpowerscripts:CreateUnlockedPackage -v devhub -n redhat-trigger-framework -k ${{ secrets.PACKAGE_INSTALL_KEY }} --branch ${{ github.head_ref }} --waittime 20
          cp $HOME/.local/share/sfdx/node_modules/@dxatscale/sfpowerscripts/scripts/readVars.sh readVars.sh
          source readVars.sh
          echo "https://test.salesforce.com/packaging/installPackage.apexp?p0=${sfpowerscripts_package_version_id}" > install.txt
        env:
          SFDX_PROJECT_AUTOUPDATE_DISABLE_FOR_PACKAGE_VERSION_CREATE: true

      - name: 'Upload install artifact'
        uses: actions/upload-artifact@v2
        with:
          name: install
          path: install.txt

      # Housekeeping
      - name: 'Delete scratch org'
        if: always()
        run: 'sfdx force:org:delete -p -u scratch-org'

  # Run Sonarscan
  static-code-scan:
    runs-on: ubuntu-latest
    needs: scratch-org-test
    steps:
      # Checkout the code in the pull request
      - name: 'Checkout source code'
        uses: actions/checkout@v2

      # Download test run artifacts
      - name: 'Get Test and Code Coverage Reports'
        uses: actions/download-artifact@v2
        with:
          path: tests/apex

      # Run Sonarscan and report the results to the PR
      - name: 'SonarCloud Analysis'
        uses: sonarsource/sonarcloud-github-action@master
        env:
            SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
            GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: >
            -Dsonar.apex.coverage.reportPath=tests/apex/codecoverage/test-result-codecoverage.json
            -Dsonar.junit.reportPaths=test/apex/test-result/test-result.xml